# Proxy Server

Use the [Proxy Server specification](../service/doctype/proxy_server/SPEC.md) for its lifecycle and Atlas ownership.

Use [HTTP proxy setup](../../services/http-proxy/docs/setup.md) for configuration on the proxy VM.

Use [Wildcard TLS](wildcard-tls.md) for certificate issuance and renewal.
